#!/bin/bash
function finish {
  echo "Goodbye!"
  sudo pkill -9 -f ~/ambient/camera.py  
exit
}

trap finish INT

echo "Starting camera! /r/n "
sh ~/scripts/camStart.sh &
echo "Reading rpi Mems data:"
sh ~/scripts/rpiMemsStart.sh 



